@Library('GitLibraryGitHub') _

pipeline {
    agent any

    stages {
        stage('Parallel Stages') {
            steps {
                script {
                    //def stages = ['stage1', 'stage2', 'stage3']
                    def environments = ["development", "staging", "production"]
                    parallelStages.runParallelStages(environments)
                }
            }
        }
    }
}
